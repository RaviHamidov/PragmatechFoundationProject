let numbers = [45, 4, 9, 16, 25];

for (let i = 0; i < numbers.length; i++) {
    console.log(`${i}` + ")" + numbers[i])
}
console.log("Umumi arrayin uzunlugu ise ( Yeni elementlerin sayi ) => "  , numbers.length)
